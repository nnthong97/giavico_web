# planning/av/ – AV (Nha Đam / Aloe Vera)

Placeholder – sẽ phát triển sau khi có file phân tích AV排程.

Dự kiến chứa:
- `data/av-mock.data.ts` – dữ liệu AV (đơn hàng, tồn kho BTP, lịch SX)
- `models/av.model.ts` – AV-specific types
- `utils/av-scheduler.ts` – backward scheduler cho AV (tính từ ngày giao ngược lại)
- `data-access/av.service.ts` – AV Angular service
